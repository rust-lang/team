org = "rust-lang"
name = "blog.rust-lang.org"
description = "Home of the Rust and Inside Rust blogs"
homepage = "https://blog.rust-lang.org"
bots = ["rustbot", "renovate"]

[pages]
build-type = "workflow"

[access.teams]
foundation-board-project-directors = "write"
leadership-council = "write"
leads = "write"
program = "write"
project-group-leads = "write"
website = "maintain"
# For Rust release blog posts
release = "write"
# For security blog posts
# Admin allows security-response members to merge without requiring
# review from maintainers, though they *should* at least get other project
# members to review
security-response = "admin"

[[rulesets]]
pattern = "main"
ci-checks = [
    "CI"
]
require-conversation-resolution = true
# Allow promote release to push without PRs
bypass-apps = ["promote-release"]
merge-queue = { enabled = true }

[environments.github-pages]
branches = ["gh-readonly-queue/main/*"]
